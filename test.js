let startDate = true
let currentDate = ''

let getDayDifferenceBetweenDates = (startDate, currentDate) => {
    return false;
}

const currentDuration = getDayDifferenceBetweenDates(startDate, currentDate) || 1;

console.log(currentDuration)
